import { Box } from '@mui/material';
import React from 'react';

const Main: React.FC = () => {
	return (
		<Box sx={{ pt: 4 }}>
			Информация о сервисе? Строка поиска? Список всех категорий?
		</Box>
	);
};

export default Main;
